node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example

# Local dev SQLite DB for /notes (production path is set via NOTES_DB_PATH,
# on a Coolify volume outside the repo)
/data

vite.config.js.timestamp-*
vite.config.ts.timestamp-*
